# This is NOT the <framework> you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `<path-to-local-docs>` before writing any code. Heed deprecation notices.
