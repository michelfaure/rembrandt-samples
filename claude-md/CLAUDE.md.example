@AGENTS.md

# <Project name> — Claude instructions

## Stack
- <Framework> <exact version> (default conventions: list those that bear on the code)
- <DB> (no ORM / ORM X)
- TypeScript strict, <CSS>
- Auth: <auth scheme + where the verification happens>

## Commands
- Dev: `<command>`
- Build: `<command>`
- Type check: `<command>`

## Conventions
- <Cross-cutting convention 1 — write positively when it's short and unambiguous>
- <Cross-cutting convention 2 — write negatively when it encodes an incident>
- <Usage rule for a client / lib whose wrong choice has already crashed>

## Module architecture
- `/app/<module-1>/` — <one-line role>
- `/app/<module-2>/` — <one-line role>
- `/lib/` — <shared helpers>

## Vertical rules
The rules of a heavy module live in `.claude/rules/<module>.md`. They are loaded
only if a task touches that module.

## Do not touch without human review
- `.env.local`
- `lib/<sensitive-file>.ts`
- Existing policies / migrations
- `/api/cron/` (scheduled jobs)
- Critical tables: <list>
